{
  "name": "@pokemon-vault/config",
  "version": "0.1.0",
  "private": true,
  "description": "Centralized validated configuration, feature flags and cron schedules (§106-108). Plain CommonJS + hand-written types — no build step.",
  "main": "src/index.js",
  "types": "src/index.d.ts",
  "scripts": {
    "lint": "eslint",
    "typecheck": "tsc --noEmit",
    "test": "node --test src/index.spec.js"
  }
}
