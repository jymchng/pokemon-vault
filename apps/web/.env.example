# Pokémon Vault — Web (Next.js) environment
# Copy to .env and adjust.

# Prisma datasource (SQLite currently; migrating to PostgreSQL via the API)
DATABASE_URL="file:./dev.db"

# Optional: disable mock fallback when the DB is unavailable
NEXT_PUBLIC_MOCK_FALLBACK="true"
