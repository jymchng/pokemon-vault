{
  "name": "@pokemon-vault/worker",
  "version": "0.1.0",
  "private": true,
  "description": "Pokémon Vault background worker (BullMQ). Scaffold in progress.",
  "scripts": {
    "dev": "tsx src/main.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.9.1",
    "@prisma/client": "^7.9.1",
    "bullmq": "^6.1.0",
    "ioredis": "^6.0.0",
    "reflect-metadata": "^0.2.2",
    "tsx": "^4.23.12",
    "@prisma/client-runtime-utils": "^7.9.1"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.9.3"
  }
}
